# Versions verified present on PyPI. torch is NOT here — it comes from the
# L4T/dustynv base image and must not be pulled from PyPI (wrong ABI, no CUDA).
fastapi==0.139.2
uvicorn[standard]==0.51.0
psycopg[binary,pool]==3.3.4
boto3==1.43.55
jinja2==3.1.6
python-multipart==0.0.32
pillow==12.3.0
pytest==9.1.1
httpx==0.28.1
