# Floor pins, not exact: the Jetson's arm64 wheel index (piwheels) lags PyPI,
# so exact pins to a just-released version fail with "no matching distribution".
# These floors are old enough to be mirrored everywhere; pip takes the newest
# available at or above them. torch is NOT here — it comes from the L4T/dustynv
# base image and must not be pulled from PyPI (wrong ABI, no CUDA).
fastapi>=0.110
uvicorn[standard]>=0.29
psycopg[binary,pool]>=3.1
boto3>=1.34
jinja2>=3.1
python-multipart>=0.0.9
pillow>=10.2
pytest>=8.0
httpx>=0.27
